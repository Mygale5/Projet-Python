import core
from core import Texture
import pygame
from pygame.math import *

temp = [0, 1, 2]
temp.append(1)
temp.append(2)
print(temp)
temp.remove(2)
print(temp)

def remove_list(l, val):

'''
def setup():
    core.fps = 30
    core.WINDOW_SIZE = [720,720]
    core.memory("obj", Texture("S3\Asteroids\Assets\projectile.png"))

def run():
    core.Texture.load(core.memory("obj"))
    core.Texture.show(core.memory("obj"))

core.main(setup, run)
'''